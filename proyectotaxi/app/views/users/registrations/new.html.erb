<!DOCTYPE html>
<html lang="es">
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<title>Login</title>
    <link rel="stylesheet" href="/assets/bootstrap.min.css" media="all" type="text/css">
</head>
<body>
<div class="container">
<h2 class="text-center"><strong>Nuevos Clientes</strong></h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, label: 'Correo Electronico',required: true, autofocus: true %>
    <%= f.input :password, required: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length), label: 'Constraseña' %>
    <%= f.input :password_confirmation, required: true, label: 'Para confirmar la Constraseña' %>
    <%= f.input :username, label: 'Nombres y Apellidos' %>
    <%= f.input :telefono, label: 'Telefono' %>
    <%= f.input :movil, label: 'Celular' %>
    <%= f.input :direccion, label: 'Dirección' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Sign up" %>
  </div>
<% end %>

<%= render "users/shared/links" %>
</div>
</body>
</html>
